#pragma once

// ---------- Wi-Fi ----------
// Copie este arquivo para "config.h" (sem o .example) e preencha com
// os seus dados reais. O config.h de verdade fica listado no .gitignore
// e nunca sobe para o repositorio.
#define WIFI_SSID     "SEU_WIFI"
#define WIFI_PASSWORD "SUA_SENHA"

// ---------- MQTT (HiveMQ Cloud) ----------
// Host, porta 8883 (TLS), usuario e senha vêm do painel do cluster
// em Access Management no site do HiveMQ Cloud.
#define MQTT_BROKER    "xxxxx.s1.eu.hivemq.cloud"
#define MQTT_PORT      8883
#define MQTT_USERNAME  "seu_usuario_hivemq"
#define MQTT_PASSWORD  "sua_senha_hivemq"
#define MQTT_TOPIC     "calculadora_agricola/leituras"
#define MQTT_CLIENT_ID "esp32_no_sensor_01"

// ---------- Sensor ----------
#define DHTPIN  4
#define DHTTYPE DHT22

// ---------- Timing ----------
// 30 minutos, conforme metodologia do TCC (intervalo fixo de amostragem)
#define READ_INTERVAL_MS 1800000UL

// ---------- NTP (hora real) ----------
#define NTP_SERVER     "pool.ntp.org"
#define GMT_OFFSET_SEC -10800   // UTC-3 (horario de Brasilia, sem horario de verao)
#define DAYLIGHT_OFFSET_SEC 0
